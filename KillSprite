public class KillSprite extends LivingSprite
{
  public KillSprite (double left, double top, int width, int height, String image,
                      double velocityX, double velocityY)
  {
    super (left, top, width, height, image, velocityX, velocityY);
  }
  
  public void step(World world)
  {
  }
}
